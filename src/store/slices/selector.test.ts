// import { DEFAULT_SORTING, NameSpace } from '../../consts/consts';
// import { getSortingOption } from './selectors';

// describe('getSortingOption selectors', () => {
//   const state = {
//     [NameSpace.Offers]: { sortingOption: DEFAULT_SORTING },
//   };

//   it('should return sortingType string from state', () => {
//     const {sortingOption} = state[NameSpace.Offers];
//     const result = getSortingOption(state);
//     expect(result).toBe(sortingOption);
//   });
// });
